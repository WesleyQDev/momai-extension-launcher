{
  "name": "momai-open",
  "version": "1.1.1",
  "private": true,
  "description": "Launcher skill for MomAI: open programs, folders and files on the local machine.",
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}

